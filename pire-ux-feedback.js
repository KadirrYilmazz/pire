(()=>{
  'use strict';
  if(window.__PIRE_UX_FEEDBACK__)return;

  const QUICK_ACTIONS=[
    {key:'students',label:'Öğrenciler',icon:'👤',primary:['Öğrenciler','Students']},
    {key:'lessons',label:'Dersler',icon:'📅',primary:['Takvim','Dersler','Calendar','Lessons']},
    {key:'packages',label:'Paketler',icon:'🎟️',primary:['Finans','Finance'],secondary:['Ders Paketleri','Lesson Packages']},
    {key:'payments',label:'Ödemeler',icon:'₺',primary:['Finans','Finance'],secondary:['Ödemeler','Ödeme Takibi','Payments','Payment Tracking']},
    {key:'notifications',label:'Bildirimler',icon:'🔔',notification:true},
    {key:'reports',label:'Raporlar',icon:'📊',primary:['Raporlar','Reports']}
  ];
  const WRITE_LABELS={
    '/api/students':'Öğrenci kaydı','/api/lessons':'Ders kaydı','/api/packages':'Paket kaydı','/api/finance':'Ödeme işlemi','/api/expenses':'Gider kaydı','/api/attendance':'Yoklama kaydı','/api/makeups':'Telafi kaydı','/api/settings':'Ayarlar','/api/announcements':'Duyuru','/api/notifications':'Bildirim ayarı','/api/customers':'Müşteri kaydı'
  };
  const normalize=value=>String(value??'').trim().toLocaleLowerCase('tr-TR').normalize('NFD').replace(/[\u0300-\u036f]/g,'').replace(/ı/g,'i');
  let menu=null,button=null,statusChip=null,toastTimer=null,mounted=false;
  function isVisitorPage(){return !!document.querySelector('button,a,[role="button"]')&&[...document.querySelectorAll('button,a,[role="button"]')].some(el=>['giris yap','sign in'].includes(normalize(el.textContent)))}
  function removeTools(){document.querySelectorAll('.pire-ux-tools').forEach(el=>el.remove());menu=button=statusChip=null;mounted=false}
  function addStyle(){if(document.querySelector('style[data-pire-ux-feedback]'))return;const style=document.createElement('style');style.dataset.pireUxFeedback='true';style.textContent=`.pire-ux-tools{position:relative;display:inline-flex;align-items:center;gap:7px}.pire-ux-sync,.pire-ux-quick-btn{height:34px;border:1px solid rgba(212,180,92,.26);border-radius:11px;background:rgba(255,255,255,.055);color:inherit;display:inline-flex;align-items:center;gap:7px;padding:0 10px;font:700 11px/1 inherit;white-space:nowrap}.pire-ux-sync i{width:7px;height:7px;border-radius:50%;background:#8f9aa7}.pire-ux-sync[data-state="ok"] i{background:#49c77d}.pire-ux-sync[data-state="error"] i{background:#ef6b6b}.pire-ux-menu{position:absolute;right:0;top:42px;width:270px;padding:10px;border:1px solid rgba(212,180,92,.22);border-radius:16px;background:rgba(18,20,25,.97);z-index:10050}.pire-ux-menu[hidden]{display:none}.pire-ux-grid{display:grid;grid-template-columns:1fr 1fr;gap:7px}.pire-ux-action{min-height:58px;border:1px solid rgba(255,255,255,.08);border-radius:12px;background:rgba(255,255,255,.045);color:inherit;padding:9px}.pire-ux-toast{position:fixed;right:22px;bottom:24px;z-index:11000;padding:12px 14px;border-radius:13px;background:rgba(20,23,28,.96);color:#f5f6f8}`;document.head.appendChild(style)}
  function findByText(labels,scope=document){const wanted=labels.map(normalize);return [...scope.querySelectorAll('button,a,[role="button"],summary')].find(el=>!el.closest('.pire-ux-tools')&&wanted.includes(normalize(el.textContent)))||null}
  function openNotification(){return [...document.querySelectorAll('button,[role="button"]')].find(el=>{if(el.closest('.pire-ux-tools'))return false;const value=normalize(`${el.textContent} ${el.getAttribute('aria-label')||''} ${el.title||''}`);return value.includes('bildirim')||value.includes('notification')})||null}
  function runAction(action){menu.hidden=true;if(action.notification){openNotification()?.click();return}const primary=findByText(action.primary||[]);if(!primary)return showToast('Bu bölüm mevcut rolde kullanılamıyor.','error');primary.click();if(action.secondary)setTimeout(()=>findByText(action.secondary)?.click(),180)}
  function showToast(message,type='success'){document.querySelector('.pire-ux-toast')?.remove();clearTimeout(toastTimer);const toast=document.createElement('div');toast.className=`pire-ux-toast ${type}`;toast.textContent=message;document.body.appendChild(toast);toastTimer=setTimeout(()=>toast.remove(),3200)}
  function setStatus(state,label,title){if(!statusChip)return;statusChip.dataset.state=state;statusChip.querySelector('span').textContent=label;statusChip.title=title||label}
  function mount(){if(isVisitorPage()){removeTools();return}if(mounted)return;const navbar=document.querySelector('.navbar-tools');if(!navbar)return;mounted=true;addStyle();const wrap=document.createElement('div');wrap.className='pire-ux-tools';statusChip=document.createElement('div');statusChip.className='pire-ux-sync';statusChip.innerHTML='<i></i><span>Veriler yükleniyor</span>';button=document.createElement('button');button.type='button';button.className='pire-ux-quick-btn';button.innerHTML='<span>⚡</span><span>Hızlı</span>';menu=document.createElement('div');menu.className='pire-ux-menu';menu.hidden=true;menu.innerHTML=`<div class="pire-ux-grid">${QUICK_ACTIONS.map(a=>`<button type="button" class="pire-ux-action" data-key="${a.key}">${a.icon} ${a.label}</button>`).join('')}</div>`;wrap.append(statusChip,button,menu);navbar.appendChild(wrap);button.addEventListener('click',()=>menu.hidden=!menu.hidden);menu.querySelectorAll('[data-key]').forEach(el=>el.addEventListener('click',()=>{const a=QUICK_ACTIONS.find(x=>x.key===el.dataset.key);if(a)runAction(a)}))}
  window.addEventListener('pire:canonical-response',event=>{const d=event.detail||{},method=String(d.method||'GET').toUpperCase();if(d.status===0||Number(d.status)>=500){setStatus('error','Bağlantı sorunu');if(method!=='GET')showToast('İşlem kaydedilemedi.','error');return}if(d.ok){setStatus('ok','Güncel');if(method!=='GET')showToast(`${WRITE_LABELS[d.path]||'İşlem'} başarıyla kaydedildi.`)}else if(Number(d.status)===401)setStatus('error','Oturum gerekli')});
  window.addEventListener('pire:canonical-legacy-mirror-ready',()=>setStatus('ok','Güncel'));
  const observer=new MutationObserver(()=>mount());if(document.body){mount();observer.observe(document.body,{childList:true,subtree:true})}else document.addEventListener('DOMContentLoaded',()=>{mount();observer.observe(document.body,{childList:true,subtree:true})},{once:true});
  window.__PIRE_UX_FEEDBACK__={enabled:true,showToast};
})();